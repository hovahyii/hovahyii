/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'camo.githubusercontent.com',
            'img.buymeacoffee.com'
        ],
    },

    
};

export default nextConfig;
